// 灵知系统 - 系统设置模块
const SettingsModule = {
    init() {
        this.loadSettings();
    },

    loadSettings() {
        // 加载设置
    }
};

window.initSettings = function() {
    SettingsModule.init();
};
